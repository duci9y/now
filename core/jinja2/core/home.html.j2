{% extends 'layout.html.j2' %}

{% block body %}
  <h2>Homepage</h2>
  {# <p>{{ events[0].host.first_name }}</p> #}
  <p>Hello {{ backends.associated.first().extra_data['first_name'] }}!</p>
  <p>Hello {{ backends.associated.first().uid }}!</p>
  <p>{{ url('profile-detail', args=[user.profile.id])}}</p>
{% endblock %}
